import Ember from 'ember';

export default Ember.Route.extend({
});

// model(input) {
//   return this.get('store').push('new', {
//     title: input.title,
//     date: input.date,
//     notes: input.notes,
//   });
// }
